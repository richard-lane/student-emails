"""
Read emails

"""
import pandas as pd


def read_emails(path: str) -> pd.DataFrame:
    """
    Read an Excel spreadsheet of emails located at `path` into a pandas DataFrame

    :param path: location of the student emails Excel file
    :returns: dataframe holding all the emails + metadata

    """
    ...
